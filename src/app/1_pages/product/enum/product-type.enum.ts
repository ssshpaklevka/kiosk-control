export enum TYPE_PRODUCT_ENUM {
  TYPE = "type",
  EXTRAS = "extras",
}

export const TYPE_PRODUCT_NAME = {
  [TYPE_PRODUCT_ENUM.TYPE]: "тип",
  [TYPE_PRODUCT_ENUM.EXTRAS]: "добавку",
}

export enum VARIANT_PRODUCT_ENUM {
  BIG = "big",
  DEFAULT = "default",
}
